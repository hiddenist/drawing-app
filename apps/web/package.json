{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@libs/color-picker": "workspace:*",
    "@libs/webgl": "workspace:*"
  }
}
